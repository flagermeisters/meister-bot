services:
  meister-bot:
    build:
      context: .
      dockerfile: Dockerfile.prod
    image: meister-bot-prod:latest
    env_file:
      - .env
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    deploy:
      resources:
        limits:
          memory: 512M
